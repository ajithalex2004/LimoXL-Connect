{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "go build -o out cmd/server/main.go"
    },
    "deploy": {
        "startCommand": "./out",
        "healthcheckPath": "/health",
        "healthcheckTimeout": 100
    }
}